import processing.core.PImage;

import java.util.List;

public class WheatStump extends Stump {
    public WheatStump(
            String id,
            Point position,
            List<PImage> images) {
        super(id,position,images);
    }
}
